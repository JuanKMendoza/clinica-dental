---
import AdminLayout from "../../layouts/AdminLayout.astro";
import { supabase } from "../../lib/supabase";

// 1. AUTH (Server Side)
const accessToken = Astro.cookies.get("sb-access-token")?.value;
const refreshToken = Astro.cookies.get("sb-refresh-token")?.value;

if (accessToken && refreshToken) {
  await supabase.auth.setSession({ access_token: accessToken, refresh_token: refreshToken });
} else {
  return Astro.redirect("/admin/login");
}

// 2. DATOS PARA KPI Y RESUMEN
const { data: appointments } = await supabase
  .from("appointments")
  .select(`*, services (name), dentists (name)`)
  .order("created_at", { ascending: false });

const pending = appointments?.filter((a) => a.status === "pending").length || 0;
const confirmed = appointments?.filter((a) => a.status === "confirmed").length || 0;
const completed = appointments?.filter((a) => a.status === "completed").length || 0;

// Solo mostramos los 5 más recientes en el Dashboard
const recentActivity = appointments?.slice(0, 5) || [];
---

<AdminLayout title="Panel de Control">
  
  <div class="max-w-7xl mx-auto">
    
    <!-- WELCOME HERO -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-2">Hola, Doctor 👋</h1>
      <p class="text-gray-500">Aquí tienes el resumen de tu clínica hoy.</p>
    </div>

    <!-- KPIs GRIDS -->
    <!-- KPIs / RESUMEN -->
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
        <!-- Total -->
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-between h-28 transition-all hover:shadow-md">
            <div class="flex justify-between items-start">
                <span class="text-xs font-bold text-gray-400 uppercase tracking-wider">Total</span>
                <div class="p-1.5 bg-gray-50 rounded-lg text-gray-500">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" /></svg>
                </div>
            </div>
            <span class="text-3xl font-bold text-gray-900">{appointments?.length || 0}</span>
        </div>

        <!-- Pendientes -->
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-between h-28 transition-all hover:shadow-md hover:border-yellow-200">
            <div class="flex justify-between items-start">
                <span class="text-xs font-bold text-yellow-600/80 uppercase tracking-wider">Pendientes</span>
                <div class="p-1.5 bg-yellow-50 rounded-lg text-yellow-600">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                </div>
            </div>
            <span class="text-3xl font-bold text-gray-900">{pending}</span>
        </div>

        <!-- Confirmadas -->
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-between h-28 transition-all hover:shadow-md hover:border-blue-200">
            <div class="flex justify-between items-start">
                <span class="text-xs font-bold text-blue-600/80 uppercase tracking-wider">Confirmadas</span>
                <div class="p-1.5 bg-blue-50 rounded-lg text-blue-600">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M14 10h4.764a2 2 0 011.789 2.894l-3.5 7A2 2 0 0115.263 21h-4.017c-.163 0-.326-.02-.485-.06L7 20m7-10V5a2 2 0 00-2-2h-.095c-.5 0-.905.405-.905.905 0 .714-.211 1.412-.608 2.006L7 11v9m7-10h-2M7 20H5a2 2 0 01-2-2v-6a2 2 0 012-2h2.5" /></svg>
                </div>
            </div>
            <span class="text-3xl font-bold text-gray-900">{confirmed}</span>
        </div>

        <!-- Completadas -->
        <div class="bg-white p-5 rounded-2xl shadow-sm border border-gray-100 flex flex-col justify-between h-28 transition-all hover:shadow-md hover:border-emerald-200">
            <div class="flex justify-between items-start">
                <span class="text-xs font-bold text-emerald-600/80 uppercase tracking-wider">Completadas</span>
                <div class="p-1.5 bg-emerald-50 rounded-lg text-emerald-600">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                </div>
            </div>
            <span class="text-3xl font-bold text-gray-900">{completed}</span>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      
      <!-- ACCESOS RÁPIDOS -->
      <div class="bg-gradient-to-br from-blue-600 to-indigo-700 rounded-3xl p-6 text-white shadow-xl lg:col-span-1">
        <h3 class="text-lg font-bold mb-4">Accesos Rápidos</h3>
        <div class="space-y-3">
          <a href="/admin/citas" class="flex items-center gap-3 p-3 bg-white/10 hover:bg-white/20 rounded-xl transition-all border border-white/10 backdrop-blur-sm group">
            <div class="w-10 h-10 rounded-lg bg-white text-blue-600 flex items-center justify-center shadow-sm">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5"><path d="M10.75 4.75a.75.75 0 00-1.5 0v4.5h-4.5a.75.75 0 000 1.5h4.5v4.5a.75.75 0 001.5 0v-4.5h4.5a.75.75 0 000-1.5h-4.5v-4.5z" /></svg>
            </div>
            <div>
              <p class="font-bold text-sm">Nueva Cita</p>
              <p class="text-xs text-blue-100 group-hover:text-white">Agendar paciente</p>
            </div>
          </a>

          <a href="/admin/configuracion" class="flex items-center gap-3 p-3 bg-white/10 hover:bg-white/20 rounded-xl transition-all border border-white/10 backdrop-blur-sm group">
            <div class="w-10 h-10 rounded-lg bg-white text-purple-600 flex items-center justify-center shadow-sm">
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" class="w-5 h-5"><path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1.323l3.954 1.582 1.599-.8a1 1 0 01.894 1.79l-1.233.616 1.738 5.42a1 1 0 01-.285 1.05A3.989 3.989 0 0115 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.715-5.349L11 6.477V16h2a1 1 0 110 2H7a1 1 0 110-2h2V6.477L6.237 7.582l1.715 5.349a1 1 0 01-.285 1.05A3.989 3.989 0 015 15a3.989 3.989 0 01-2.667-1.019 1 1 0 01-.285-1.05l1.738-5.42-1.233-.617a1 1 0 01.894-1.788l1.599.799L9 4.323V3a1 1 0 011-1zm-5 8.274l-.818 2.552c.25.112.526.174.818.174.292 0 .569-.062.818-.174L5 10.274zm10 0l-.818 2.552c.25.112.526.174.818.174.292 0 .569-.062.818-.174L15 10.274z" clip-rule="evenodd" /></svg>
            </div>
            <div>
              <p class="font-bold text-sm">Doctores</p>
              <p class="text-xs text-blue-100 group-hover:text-white">Gestionar equipo</p>
            </div>
          </a>
        </div>
      </div>

      <!-- ÚLTIMA ACTIVIDAD (Solo Lectura) -->
      <div class="bg-white rounded-3xl shadow-sm border border-gray-100 lg:col-span-2 overflow-hidden flex flex-col">
        <div class="p-6 border-b border-gray-50 flex justify-between items-center">
          <h3 class="font-bold text-gray-900">Actividad Reciente</h3>
          <a href="/admin/citas" class="text-xs font-bold text-blue-600 hover:text-blue-800">Ver todo →</a>
        </div>
        
        <div class="flex-1 overflow-x-auto">
          <table class="w-full text-sm text-left">
            <thead class="bg-gray-50 text-gray-500 font-medium">
              <tr>
                <th class="px-6 py-3">Paciente</th>
                <th class="px-6 py-3">Fecha</th>
                <th class="px-6 py-3">Estado</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-50">
              {recentActivity.map(apt => (
                <tr class="hover:bg-gray-50/50 transition-colors">
                  <td class="px-6 py-3 font-medium text-gray-900">{apt.customer_name}</td>
                  <td class="px-6 py-3 text-gray-500">
                    {new Date(apt.appointment_date + 'T00:00:00').toLocaleDateString('es-CO', {day: 'numeric', month: 'short'})} 
                    <span class="text-xs ml-1 bg-gray-100 px-1.5 py-0.5 rounded text-gray-600">{apt.appointment_time.slice(0,5)}</span>
                  </td>
                  <td class="px-6 py-3">
                    {apt.status === "pending" && <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">Pendiente</span>}
                    {apt.status === "confirmed" && <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">Confirmada</span>}
                    {apt.status === "completed" && <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">Completada</span>}
                    {apt.status === "cancelled" && <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600">Cancelada</span>}
                  </td>
                </tr>
              ))}
              {recentActivity.length === 0 && (
                <tr><td colspan="3" class="px-6 py-8 text-center text-gray-400">No hay actividad reciente.</td></tr>
              )}
            </tbody>
          </table>
        </div>
      </div>

    </div>

  </div>
</AdminLayout>